Simple python script that reads the metadata of photos and videos, and organises them into folders by years.

Uses TinyTag, PIL, PIL.ExifTags 

Supported formats:

`	           `MP3 / MP2 / MP1 (ID3 v1, v1.1, v2.2, v2.3+) 

`               `M4A (AAC / ALAC)

`               `WAVE / WAV

`               `OGG (FLAC / Opus / Speex / Vorbis)

`               `FLAC

`               `WMA

`               `AIFF / AIFF-C

`               `JPG

`               `JPEG

`               `PNG

`               `GIF

